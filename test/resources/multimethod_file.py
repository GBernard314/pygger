def method1(chaine:str, entier: int, flottant: float, liste: list, liste_string: list, dictionnaire: dict):
    """This is a test function for lot of inputs

    Args:
        chaine (str): a string
        entier (int): an int
        flottant (float): a float
        liste (list): a list
        liste_string (list[string]): a list of string
        dictionnaire (dict): a dictionnary
    """
    return

def method2() -> list:
    """This is a test function having 
    0 input and 1 output

    Returns:
        list: a list
    """